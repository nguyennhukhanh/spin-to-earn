import { type LucideProps } from 'lucide-react';
import React from 'react';

import { Icons } from '@/assets/icons';

const AvailableNetwork = (props: LucideProps) => {
  return <Icons.availableNetwork {...props} />;
};

export { AvailableNetwork };
