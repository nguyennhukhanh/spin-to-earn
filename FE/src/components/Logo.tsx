import React from 'react';

import { type SVGElementProps } from '@/types/common.type';

interface Props extends SVGElementProps {}

const Logo = ({ ...props }: Props) => {
  return <>Logo</>;
};

export { Logo };
